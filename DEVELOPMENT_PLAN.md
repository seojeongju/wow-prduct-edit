# 스마트 상세페이지 매니저 개발 로드맵

## 🎯 개발 목표
복잡한 상세페이지 제작 과정을 **4단계 위저드(Wizard)** 형식으로 단순화하여, 사용자가 순서대로 따라하기만 하면 완성되는 최적의 UX 구현.

## 🚀 단계별 구현 계획

### Step 1. 기획 및 카피 생성 (Planning & Copy)
**"상품의 매력을 언어로 정의하는 단계"**
- **UI/UX**: 
  - 상품명, 타겟, USP(핵심장점) 입력 폼.
  - 톤앤매너 선택 (감성/신뢰/유머).
  - 'AI 기획하기' 버튼.
- **기능(API)**:
  - **OpenAI (GPT-4o)**: 입력 정보를 바탕으로 후킹 문구, 본문, 신뢰 포인트, 클로징 카피 자동 생성.
  - 생성된 문구를 사용자가 수정할 수 있는 에디터 제공.

### Step 2. 이미지 생성 및 편집 (Image Processing)
**"시각적 요소를 완성하는 단계"**
- **UI/UX**:
  - 고해상도 원본 업로드 (Drag & Drop).
  - AI 기능 선택: [배경 제거 / 배경 합성 / 이미지 확장].
  - 전/후 비교(Slider) 프리뷰.
- **기능(API)**:
  - **Remove.bg** 또는 **Cloudflare Workers AI (briaai/p-remove-bg)**: 배경 제거.
  - **OpenAI (DALL-E 3)**: 필요 시 연출컷 생성 또는 배경 합성.

### Step 3. 디자인 레이아웃 (Layout & Design)
**"구조를 잡고 스타일을 입히는 단계"**
- **UI/UX**:
  - 생성된 텍스트와 이미지를 블록(Block) 단위로 자동 배치.
  - 템플릿 테마 선택 (모던 / 럭셔리 / 팝).
  - 드래그 앤 드롭으로 블록 순서 변경.
- **기능**:
  - **Tailwind Custoim Config**: 테마별 컬러셋, 폰트 자동 적용.
  - **Framer Motion**: 부드러운 스크롤 인터랙션 미리보기.

### Step 4. 결과물 및 연동 (Export & Integration)
**"판매를 시작하는 단계"**
- **UI/UX**:
  - 최종 모바일/PC 뷰 확인.
  - HTML 복사 / 통이미지 다운로드 버튼.
  - 스마트스토어 API 연동 버튼 (선택 사항).
- **기능**:
  - **html-to-image**: 렌더링된 화면을 고화질 PNG로 변환.
  - **Clipboard API**: 네이버 에디터 호환 HTML 코드 복사.

---

## 🛠 실행 순서 (Action Plan)

1. **Phase 1: UI 프레임워크 (현재 작업)**
   - `zustand` 상태 관리 라이브러리 도입 (단계별 데이터 유지).
   - 메인 페이지를 Step 기반 위저드 UI로 리팩토링.
   
2. **Phase 2: [Step 1] 기획/카피 기능 고도화**
   - 백엔드 Prompt Engineering 최적화.
   - 문구 수정 UI 구현.

3. **Phase 3: [Step 2] 이미지 처리 구현**
   - 이미지 업로드 및 배경 제거 API 연동.

4. **Phase 4: [Step 3 & 4] 디자인 및 내보내기**
   - 템플릿 시스템 구축 및 다운로드 기능 구현.
